<h2><%= @package.title %></h2>

<% [
     :creator,
     :title,
     # :date deliberately missing
     :access,
     :retention,
     # optional fields follow
     :contributor,
     :coverage,
     :description,
   ].each do |field| -%>
<p>
  <b><%= field.to_s.humanize %>:</b>
  <%= @package.send(field) %>
</p>
<% end -%>

<p>
  <b>Format:</b>
  <%= render(@package.format) if @package.format %>
</p>

<%# :identifier deliberately missing %>

<p>
  <b>Language:</b>
  <%= render(@package.language) if @package.language %>
</p>

<% [
     :publisher,
     :relation,
     :rights,
     :source,
   ].each do |field| -%>
<p>
  <b><%= field.to_s.humanize %>:</b>
  <%= @package.send(field) %>
</p>
<% end -%>

<% if @subjects -%>
<p>
  <b>Subjects:</b> <br/ >
  <ul>
    <% @subjects.each do |subject| -%>
    <li><%= subject.subject %></li>
    <% end -%>
  </ul>
</p>
<% end -%>

<p>
  <b>Type:</b>
  <%= render(@package.type) if @package.type %>
</p>

<%= link_to 'Edit', edit_package_path(@package) %> |
<%= link_to 'Back', packages_path %>
